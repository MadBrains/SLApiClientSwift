//
// Promocode.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation


/** Объект запроса промокода */

open class Promocode: Codable {

    /** Промокод */
    public var code: String
    /** Список товаров */
    public var items: [PromocodeItem]
    /** Идентификатор */
    public var shopId: UUID


    public init(code: String, items: [PromocodeItem], shopId: UUID) {
        self.code = code
        self.items = items
        self.shopId = shopId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {

        var container = encoder.container(keyedBy: String.self)

        try container.encode(code, forKey: "code")
        try container.encode(items, forKey: "items")
        try container.encode(shopId, forKey: "shopId")
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: String.self)

                code = try container.decode(String.self, forKey: "code")
                items = try container.decode([PromocodeItem].self, forKey: "items")
                shopId = try container.decode(UUID.self, forKey: "shopId")
    }
}

