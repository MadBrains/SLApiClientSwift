//
// ServiceGroupsResponse.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation


/** Ответ на запрос о списке объектов в группе или корне. Если запрос по корневой группе то group не указан в ответе */

open class ServiceGroupsResponse: Codable {

    public var group: ServiceGroup?
    /** Услуги */
    public var items: [ServiceItem]?
    /** Группы */
    public var subGroups: [ServiceGroup]?


    public init(group: ServiceGroup?, items: [ServiceItem]?, subGroups: [ServiceGroup]?) {
        self.group = group
        self.items = items
        self.subGroups = subGroups
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {

        var container = encoder.container(keyedBy: String.self)

        try container.encodeIfPresent(group, forKey: "group")
        try container.encodeIfPresent(items, forKey: "items")
        try container.encodeIfPresent(subGroups, forKey: "subGroups")
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: String.self)

                group = try container.decodeIfPresent(ServiceGroup.self, forKey: "group")
                items = try container.decodeIfPresent([ServiceItem].self, forKey: "items")
                subGroups = try container.decodeIfPresent([ServiceGroup].self, forKey: "subGroups")
    }
}

