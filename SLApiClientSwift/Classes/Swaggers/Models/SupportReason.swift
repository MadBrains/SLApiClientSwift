//
// SupportReason.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation


/** Причина обратной связи */

open class SupportReason: Codable {

    /** Название на латиннице (^[0-9a-zA-Z-_]+$) */
    public var name: String
    public var properties: [IProperty]?
    /** Название для отображение в пользовательском интерфейсе */
    public var title: String


    public init(name: String, properties: [IProperty]?, title: String) {
        self.name = name
        self.properties = properties
        self.title = title
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {

        var container = encoder.container(keyedBy: String.self)

        try container.encode(name, forKey: "name")
        try container.encodeIfPresent(properties, forKey: "properties")
        try container.encode(title, forKey: "title")
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: String.self)

                name = try container.decode(String.self, forKey: "name")
                properties = try container.decodeIfPresent([IProperty].self, forKey: "properties")
                title = try container.decode(String.self, forKey: "title")
    }
}

