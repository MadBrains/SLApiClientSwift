//
// ShininvestTimeQuery.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation


/** Объект запроса поиска временных слотов. Идентификаторы shops выбираются  через объединение (or), resources игнорируются. Необходимо передать как  минимум один shops.  */

open class ShininvestTimeQuery: Codable {

    /** Список дат для выборки по RFC 3339, section 5.6, for example, 2017-07-21 */
    public var dates: [String]
    /** Массив идентификаторов ресурсов */
    public var resources: [String]?
    /** ID услуги, для которой нужно получить временные слоты */
    public var serviceId: UUID
    /** Массив идентификаторов точек продаж */
    public var shops: [String]


    public init(dates: [String], resources: [String]?, serviceId: UUID, shops: [String]) {
        self.dates = dates
        self.resources = resources
        self.serviceId = serviceId
        self.shops = shops
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {

        var container = encoder.container(keyedBy: String.self)

        try container.encode(dates, forKey: "dates")
        try container.encodeIfPresent(resources, forKey: "resources")
        try container.encode(serviceId, forKey: "serviceId")
        try container.encode(shops, forKey: "shops")
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: String.self)

                dates = try container.decode([String].self, forKey: "dates")
                resources = try container.decodeIfPresent([String].self, forKey: "resources")
                serviceId = try container.decode(UUID.self, forKey: "serviceId")
                shops = try container.decode([String].self, forKey: "shops")
    }
}

